CS2 M4A1 Mecha Industries Field Tested price data import via Skinport API 

Gets price data for M4A1 and sends an email to specified address at daily schedule.

GitHub Actions runs the scirpt
        ->
Script imports data from Skinport
        ->
Script sends an e-mail from the chosen sender account
        ->
Price update is received as an daily alert via email
